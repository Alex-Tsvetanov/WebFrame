% 07_application
\section{\bgen{ПРИЛОЖЕНИЕ И ВАЛИДАЦИЯ}{APPLICATION AND VALIDATION}}

Библиотека може да бъде бърза и въпреки това неизползваема. Тази глава проверява втората
половина: дали архитектурните решения от глава~III остават поносими, когато с тях се пише
приложение, а не измерителен стенд.

Тези решения не са неутрални спрямо кода на приложението. Демултиплексирането обещава, че
добавянето на протокол не разклонява приложението; отложеното получаване въвежда нов тип в
модела на изгледа. И двете твърдения се проверяват най-добре, като се напише нещо с тях.

\subsection{Примерното приложение}

Приложението е списък със задачи с потребители. То се състои от 1790 реда на C++ в
\code{examples/Project}, 206 реда шаблони и 846 реда JavaScript и CSS за страницата.
Броенето е с \code{wc -l} върху изходните файлове, тоест включва коментари и празни редове.
Организацията е по предназначение, а не по тип на файла.

\begin{table}[H]
\centering
\caption{Организация на примерното приложение}
\label{tab:example-app}
% Първата колона е по-широка от нужното за повечето редове, но не и за
% src/handlers/websocket: \texttt не се пренася, така че тясна колона го изкарва
% извън полето, вместо да го пренесе.
\begin{tabular}{@{}L{4.9cm}L{9.3cm}@{}}
\toprule
\textbf{Директория} & \textbf{Съдържание} \\
\midrule
\code{src/app} & Конфигурация и съставяне на сървъра. \\
\code{src/handlers/api} & API маршрути за задачи и потребители. \\
\code{src/handlers} & Маршрути за страници, тоест изгледи. \\
\code{src/handlers/websocket} & Двупосочен канал за известия при промяна на задача. \\
\code{src/middleware} & Журналиране на заявките и удостоверяване. \\
\code{src/models}, \code{src/services} & Данни и логика, независими от протокола. \\
\code{templates} & Шаблони за страници и общо оформление. \\
\code{static} & CSS и JavaScript на страницата. \\
\code{tests} & Празна. Виж бележката по-долу. \\
\bottomrule
\end{tabular}
\end{table}

Редът за \code{tests} е записан такъв, какъвто е. Директориите съществуват, съдържат само
\code{.gitkeep}, а блокът в \code{CMakeLists.txt}, който би построил тези тестове, е
коментиран заедно със списъка на осемте файла, които е трябвало да съдържа. Приложението
няма собствени тестове. Това е липса, а не решение, и се брои като такава в
раздел~\ref{ssec:app-limits}.

Разделянето на \code{handlers/api} от \code{handlers} за страници не е наложено от
библиотеката като директория, а следва от разделянето на маршрутите: API маршрутите и
маршрутите за изгледи се регистрират поотделно и се обслужват от отделни маршрутизатори.
Приложението просто отразява това разделение в структурата си.

\subsubsection{Маршрутите}

Маршрутите са тринадесет HTTP и един WebSocket, регистрирани от четири места, плюс
статичните файлове, които минават през middleware, а не през маршрутизатора. Пълният
списък е в таблица~\ref{tab:example-routes}, защото твърденията по-надолу се позовават на
конкретни редове от него.

\begin{table}[H]
\centering
\caption{Маршрутите на примерното приложение}
\label{tab:example-routes}
\begin{tabular}{@{}L{4.4cm}L{7.4cm}L{2.0cm}@{}}
\toprule
\textbf{Маршрут} & \textbf{Какво прави} & \textbf{Иска сесия} \\
\midrule
\code{GET /} & Изобразява \code{pages/index.html}. & не \\
\code{GET /login} & Изобразява формата; пренасочва към \code{/}, ако вече има сесия. & не \\
\code{GET /logout} & Пренасочва и изтрива двете бисквитки. & не \\
\midrule
\code{POST /api/login} & Проверява данните и поставя \code{user\_id} и \code{username}. & не \\
\code{POST /api/logout} & Изтрива същите бисквитки. & не \\
\code{GET /api/me} & Текущият потребител по \code{user\_id}. & да \\
\code{GET /api/users} & Списък за падащото меню при възлагане. & не \\
\midrule
\code{GET /api/stats/tasks} & Броячи по състояние. & не \\
\code{GET /api/tasks} & Списък с филтри и странициране. & не \\
\code{POST /api/tasks} & Създава задача. & да \\
\code{GET /api/tasks/\{id\}} & Една задача или 404. & не \\
\code{PUT /api/tasks/\{id\}} & Променя задача. & да \\
\code{DELETE /api/tasks/\{id\}} & Изтрива задача. & да \\
\midrule
\code{WS /ws} & Каналът за известия. & не \\
\code{GET /static/...} & Статични файлове, през middleware. & не \\
\bottomrule
\end{tabular}
\end{table}

Един ред от таблицата е следа от ограничение на библиотеката, а не избор на приложението.
Статистиката се обслужва от \code{/api/stats/tasks}, а не от естественото
\code{/api/tasks/stats}, и изходният текст записва причината на самото място: за да не се
сблъска с \code{/api/tasks/\{id\}}. Тоест авторът е преименувал маршрут, вместо да разчита,
че постоянният сегмент има предимство пред параметъра. В тестовете на маршрутизатора няма
случай, който да фиксира кой от двата печели, тоест не е установено дали маршрутизаторът
не може да ги различи, или просто не е бил опитан. Това е находка, която се появява само
когато някой напише приложение.

\subsubsection{Услуги и модели}

Услугите държат състоянието и не знаят нищо за HTTP. \code{TaskService} и
\code{UserService} го пазят в \code{std::unordered\_map} под \code{std::mutex} и
предлагат обикновени методи: \code{create}, \code{find}, \code{list}, \code{update},
\code{remove}, \code{authenticate}. Няма база от данни, тоест никоя услуга не изчаква
истински вход-изход. Това е най-сериозното ограничение на примера.

Моделите носят три функции: \code{to\_json}, \code{from\_json} и
\code{validate}. Последната връща \code{std::optional<std::string>}, тоест празна стойност
при валидни данни и съобщение при невалидни, което handler-ът превръща в отговор 400. Така
проверката на входа стои в модела, а превръщането ѝ в отговор стои в handler-а.
\code{User::to\_json} изрично пропуска \code{password\_hash}, с коментар на реда.
Филтрирането е също модел: handler-ът чете параметрите на заявката и попълва
\code{TaskFilter}, а сортирането и странирането се извършват в услугата.

Съставянето става в конструктора на \code{Server} и редът има значение, отбелязан с
коментар в изходния текст: първо \code{TaskHub}, после \code{UserService}, накрая
\code{TaskService}, който получава hub-а по препратка. Тази препратка е единствената връзка
между HTTP частта и канала за известия.

\subsubsection{Каналът за известия и връзката му с HTTP маршрутите}

Каналът не е паралелен на API-то, а е закачен зад него. \code{TaskService::create},
\code{update} и \code{remove} извикват съответно \code{broadcast\_created},
\code{broadcast\_updated} и \code{broadcast\_deleted}, след като са освободили мутекса.
Тоест известието тръгва от услугата, а не от handler-а, и се появява независимо от това по
кой маршрут е дошла промяната.

\code{TaskHub} не изпраща сам. Той поставя съобщението в опашка за всяка от отворените
връзки, а изпращането се извършва от корутината на самата връзка: тя изпразва опашката си,
преди да изчака следващото съобщение. Следствието е неудобно и трябва да се каже. Мълчалив
клиент получава известието едва когато неговият цикъл се върне от изчакването, тоест едва
когато сам изпрати нещо. Кодът на
страницата решава това, като изпраща \code{ping} на всяка секунда, и коментарът в
\code{static/js/app.js} го заявява дословно: ping-ът е там, за да накара сървъра да изпрати
чакащите съобщения. Тоест каналът, наречен „в реално време“, на практика се допитва с
честота 1~Hz. Известието не се разпространява от сървъра, а се изтегля от клиента. Това е
дефект в примера, не свойство на библиотеката, но е дефект, който примерът не показва сам.

\subsubsection{Middleware}

Регистрирани са три компонента, в реда, в който се изпълняват. \code{request\_logger} е
най-външният: той засича времето преди и след извикването на \code{next} и отпечатва
състоянието на отговора, продължителността, метода и пътя. \code{coroute::compression}
следва. \code{coroute::static\_files} с префикс \code{/static}, изключено изброяване на
директории и \code{max\_age} от 3600 секунди е третият.

Четвъртият и петият не са регистрирани. \code{src/middleware/auth.cpp} съдържа
\code{require\_auth} и \code{load\_user}, написани и компилирани, но \code{Server} не
извиква нито едното. Вместо това всеки handler, който изисква сесия, повтаря локално една и
съща проверка върху бисквитките. Трите маршрута, които променят задача, я правят с еднакъв
код, а \code{/api/me} има свой вариант на същото.

Това е находка, а не оформление. Приложението е предпочело повторение пред middleware,
защото \code{app.use} прилага компонента върху всички маршрути, а приложението няма
механизъм за прикачване на middleware само към една група. Тоест удостоверяването не се
вписа в единствения предвиден начин за композиция и авторът го заобиколи, без да отбележи,
че го заобикаля. Точно това ограничението в раздел~\ref{ssec:app-limits} предупреждава, че
се случва.

\subsection{Трите твърдения, проверени в код}

\paragraph{Че добавянето на протокол не се вижда в приложението.} Handler-ите в
\code{src/handlers} не съдържат условия по протокол. Търсене на \code{http\_version},
\code{HTTP/2} и \code{HTTP/3} в целия \code{examples/Project/src} не връща нито едно
съвпадение. Един и същи handler обслужва заявка, пристигнала по HTTP/1.1, по HTTP/2 или по
HTTP/3, защото и трите достигат до него през един и същ маршрутизатор.

\begin{lstlisting}[language=C++,caption={Handler без нито едно условие по протокол},label={lst:app-handler}]
app.get("/api/tasks/{id}",
        [&task_service](Request& req) -> Task<Response>
        {
            auto id_result = req.param<int64_t>(0);
            if (!id_result) co_return json_error(400, "Invalid task ID");

            auto task = task_service.find(*id_result);
            if (!task) co_return json_error(404, "Task not found");

            co_return Response::json(task->to_json().dump());
        });
\end{lstlisting}

Листингът е съкратен по фигурните скоби на едноредовите тела и по пълните имена на
пространствата; останалото е дословно. В него няма нищо, зависещо от транспорта: нито
версия, нито заглавна част, нито поточен идентификатор. Твърдението от глава~III е
проверено от страната на потребителя, а не на реализацията, и потвърдено от проверката в
раздел~\ref{ssec:app-checks}, където един и същи handler отговаря и по трите протокола.

\paragraph{Че разделянето на API от изгледи не води до дублиране.} Изгледът получава
данните си от същия маршрут, който обслужва и API клиентите, и извикването не напуска
процеса.

\begin{lstlisting}[language=C++,caption={Изглед, който взема данните си от API маршрут в същия процес},label={lst:app-view}]
app.view<ListingVm>("/",
    [&app](Request&) -> View<ListingVm>
    {
        // Същият маршрут, който обслужва и външните клиенти.
        Response resp = co_await app.fetch_get("/api/items");

        std::vector<std::string> items;
        if (resp.status() == 200)
        {
            items = nlohmann::json::parse(resp.body())
                        .get<std::vector<std::string>>();
        }

        co_return ViewResult<ListingVm>{
            .templates = ViewTemplates{"listing"},
            .model = ListingVm{.title = "Items from API",
                               .items = std::move(items)}};
    });
\end{lstlisting}

\code{App::fetch} съставя заявка, съпоставя я с маршрутизатора, изпълнява веригата от
middleware и връща отговора. Няма сокет, няма системно извикване, няма кръгово пътуване.

Пресичането обаче не е безплатно и цената трябва да се назове точно: тялото се сериализира
до JSON от API маршрута и се разбира обратно от изгледа. Спестява се транспортът, не
сериализацията. Прекият вариант, при който изгледът извиква услугата и подминава маршрута,
също е възможен и спестява и двете; примерното приложение подава \code{UserService} на
регистрацията на страниците именно за това, но текущият handler за \code{/} чете само
бисквитката и не докосва услугата, така че параметърът стои маркиран
\code{[[maybe\_unused]]}. Тоест примерното приложение показва разделянето на маршрутите, но
не и вземането на данни от услуга вътре в изглед. Другият пример,
\code{examples/view\_example}, също не го показва: там изгледът минава през
\code{fetch\_get} към собствения си API маршрут, тоест през извикване без сокет, а не през
пряко обръщение към услуга. Двата пътя се различават и разликата се назовава тук, защото
по-ранна редакция на този раздел ги разменяше.

\paragraph{Че моделът на изгледа остава прост.} Моделът е обикновена структура с написана
на ръка функция за преобразуване към JSON. Няма базов клас, няма макрос за регистрация и
няма нищо, което да свързва структурата с библиотеката. Отложена стойност се добавя като
поле от тип \code{Deferred<T>}. Промяната е следната, и това е целият ѝ обем:

\begin{lstlisting}[language=C++,caption={Пълната промяна при добавяне на отложено поле},label={lst:app-deferred}]
struct Dashboard
{
    std::string title;
    int         visitors_now = 0;
    Deferred<std::string> slow_report;      // първи ред
};

void to_json(nlohmann::json& json, const Dashboard& model)
{
    json = nlohmann::json{
        {"title", model.title},
        {"visitors_now", model.visitors_now},
        {"slow_report", model.slow_report}}; // втори ред
}
\end{lstlisting}

Строго погледнато редовете са два, а не един, и вторият съществува само защото
преобразуването е написано на ръка. Той не носи знание за отлагането: изглежда точно като
реда над себе си.

Останалото се случва без участие на модела. Преобразуването на \code{Deferred<T>} проверява
дали стойността е готова; ако е, я записва, а ако не е, регистрира гнездо в събирача, който
е активен по време на изобразяването, и вписва запълнител. Тоест моделът не декларира кои
от полетата му са отложени и не съществува списък, който да остане несинхронизиран с него.

Има и трети случай, който е избор, а не пропуск: ако стойността не е готова и няма активен
събирач, полето се сериализира като \code{null}. Никой не предава по-късните части, така че
запълнител би бил обещание, което страницата никога няма да изпълни.

\subsection{Отделни примери за отделни свойства}

Освен цялото приложение съществуват и единадесет малки примера, всеки от които изолира по
едно свойство. Таблица~\ref{tab:samples} ги изброява заедно с размера им, защото главата
твърди, че те служат за изпълним отговор на въпроса „колко код струва това“, а такъв
отговор без числа не е отговор.

\begin{table}[H]
\centering
\caption{Отделните примери и техният размер}
\label{tab:samples}
% Първата колона побира точно \texttt{websocket_server}: по-тясна и трите най-дълги
% имена излизат извън полето, защото \texttt не се пренася.
\begin{tabular}{@{}L{3.7cm}L{7.9cm}r@{}}
\toprule
\textbf{Пример} & \textbf{Какво изолира} & \textbf{Редове} \\
\midrule
\code{static\_server}    & Статични файлове, ETag, изброяване на директории. & 75 \\
\code{raw\_benchmark}    & Цикълът за вход и изход и корутините без HTTP слоя отгоре. & 85 \\
\code{echo\_server}      & Тяло на заявката, спиране по сигнал. & 87 \\
\code{http2\_server}     & HTTP/2 по TLS и по открит текст. & 88 \\
\code{template\_server}  & Шаблони и потребителска функция в тях. & 91 \\
\code{https\_server}     & TLS със сертификат от команден ред. & 95 \\
\code{hello\_world}      & Маршрути, типизирани параметри, компресия. & 114 \\
\code{streaming\_server} & Отговор на части. & 168 \\
\code{range\_server}     & Частични отговори върху файл от 1~MB. & 185 \\
\code{websocket\_server} & Ехо и разпращане към няколко клиента. & 225 \\
\code{benchmark\_server} & Минимален сървър, чиито настройки са флагове. & 482 \\
\bottomrule
\end{tabular}
\end{table}

Числата са редове в \code{main.cpp} и не са мярка за нужния код. Те включват коментарите и
текста, който всеки пример отпечатва при пускане, а в няколко от тях този текст е
съществена част от файла: \code{hello\_world} изразходва десетина реда само за да
изброи адресите, които читателят да опита. Тоест таблицата дава горна граница, при това
щедра.

Долната граница е по-къса от всеки ред в таблицата. Пълен сървър, който отговаря по
HTTP/1.1, са три израза: конструиране на \code{App}, един \code{app.get} с ламбда, връщаща
\code{Response::ok}, и \code{app.run} с номер на порт. Всичко останало в
\code{hello\_world} е допълнителните свойства, които той показва.

Два от примерите не се строят винаги: \code{http2\_server} изисква
\code{COROUTE\_ENABLE\_HTTP2}, а \code{https\_server} изисква \code{COROUTE\_ENABLE\_TLS}.
Останалите девет се строят безусловно.

Двата примера за измерване са различни по предназначение от останалите девет.
\code{raw\_benchmark} заобикаля HTTP слоя и работи направо с цикъла за вход и изход на
библиотеката, чийто backend се избира при изпълнение, за да покаже колко струва слоят отгоре. При \code{benchmark\_server} всяка настройка е
флаг от командния ред, а не опция при компилиране, защото различието между два отделно
компилирани двоични файла е по-голямо от разликата, която се измерва. Сред флаговете е и
\code{-{}-no-detect}, тоест рамото, спрямо което се измерва демултиплексирането.

\subsection{Три изпълними проверки}
\label{ssec:app-checks}

Отделно от примерите съществуват три проверки, използвани в настоящата работа като
доказателство, а не като демонстрация. Всяка от тях се състои от малък сървър и скрипт,
който го пуска и проверява изхода. И трите живеят в \code{tests/integration}.

\paragraph{Един екземпляр, един номер на порт, три протокола.}
\code{verify\_multiprotocol.sh} строи \code{http3\_app.cpp}, който регистрира два маршрута
и включва TLS и HTTP/3 върху едно повикване за пускане, и го пуска с четири работни нишки,
за да се използва сегментираният път. После проверява девет неща: че \code{curl} по HTTP/1.1
получава 200 и тялото, че същото важи по HTTP/2 на същия порт, и че референтният клиент на
ngtcp2 получава договорен ALPN \code{h3}, състояние 200 и същото тяло по UDP. Проверява се
и че заглавната част \code{Alt-Svc} присъства и в двата TCP отговора, защото без нея UDP
страната е достижима на теория и не се използва на практика: браузър не опитва HTTP/3, ако
не му е казано, че съществува \cite{rfc7838}.

Накрая скриптът брои дескрипторите с \code{ss} и отпечатва броя на TCP и UDP слушателите
заедно с броя на работните нишки. Твърдението, което тази бройка проверява, е формулирано в
самия скрипт и е по-слабо от възможното: един слушащ дескриптор на транспорт на работна
нишка, независимо колко протокола се обслужват, а не един дескриптор изобщо.

\paragraph{HTTP/3 срещу чужда реализация.} \code{verify\_http3.sh} прави същото за HTTP/3
сам, срещу референтния клиент на ngtcp2. Проверяват се три низа и всеки от тях се проваля
различно: липсващият договорен ALPN значи грешен TLS контекст, липсващото състояние 200
значи, че заявката никога не е стигнала до handler, а липсващото тяло значи, че отговорът е
бил оформен, но не и изпратен. Тялото е под шестнадесет знака нарочно, защото клиентът го
отпечатва като шестнадесетичен дъмп с ASCII колона, широка шестнадесет байта.

Скриптът съществува вместо случай в \code{ctest} по причина, записана в него: нужна е втора
реализация на QUIC, а тя не е зависимост при строене. Протоколен код, който никога не е
завършвал ръкостискане срещу нещо, което не е писал сам, не заслужава доверие.

\paragraph{Че обвивката излиза преди стойността.} \code{verify\_deferred.sh} строи
\code{deferred\_app.cpp}, който изобразява страница с едно отложено поле, чието изчисляване
отнема зададен брой милисекунди. Скриптът извиква \code{curl} с изключено буфериране,
отпечатва до всеки пристигнал ред времето на пристигането му, и после сравнява два момента:
този на реда с готовата част на страницата и този на реда, който предава стойността.
Проверката минава, ако разликата е поне половината от зададеното закъснение.

Останалите шест проверки са върху съдържанието: че страницата носи и двата клиентски пътя,
този по атрибут за страница без JavaScript и този през \code{Promise}. Един детайл в
скрипта е поправка на самата измервателна процедура: търси се извикването на функцията,
която предава стойността, а не самото ѝ име, съвпадащо и с дефиницията ѝ в главата на
документа. Дефиницията пристига преди обвивката и първоначално правеше измерената разлика
отрицателна.

\paragraph{Какво не доказват.} И трите са скриптове, пускани на ръка под Linux, и нито един
не влиза в непрекъснатата интеграция. Два от тях изискват локално построени OpenSSL, ngtcp2 и
nghttp3 под отделен префикс, тоест не вървят в обикновената задача за тестване; проверката за
отлагането не изисква нито TLS, нито HTTP/3 и минава по открит текст. При двата, които
използват TLS, проверката на сертификата е изключена, защото сертификатът е самоподписан. Всичко се случва върху
локалния интерфейс, тоест без загуба на пакети, без пренареждане и без реална латентност.
Проверката за отлагането минава по праг от половината закъснение, а не по точна стойност, и
измерва времето с разделителна способност от милисекунда.

\subsection{Ограничения на тази проверка}
\label{ssec:app-limits}

Използваемостта тук се преценява по един източник, и това трябва да се каже.

Приложението е написано от автора на библиотеката. Такъв автор знае кои интерфейси са
неудобни и ги заобикаля, без да забележи, че ги заобикаля. Следователно настоящата глава
показва, че решенията \emph{могат} да бъдат използвани, а не че са удобни за някой друг.

Двете заобикаляния, описани по-горе, са точно този случай. Нито преименуваният маршрут,
нито повтореното удостоверяване са отбелязани в изходния текст като компромис; открити са
при четенето за настоящата глава, не при писането. Външен потребител не би ги заобиколил
мълчаливо, а би ги съобщил като дефекти.

Приложението не изчаква истински вход-изход. Всичко е в паметта, под мутекс. Тоест случаят,
в който архитектурата има най-голяма вероятност да заболи, изглед, изчакващ бавна заявка
към база от данни, е изпробван само от \code{deferred\_app.cpp}, където бавната стойност е
нишка, спяща зададен брой милисекунди.

Има и по-груби липси. Приложението няма собствени тестове, макар да има директории за тях.
Обработката на грешки е незапълнена: функцията, която трябва да ги регистрира, е празна, с
коментар, че за това липсва поддръжка в библиотеката, тоест приложението е установило
липсата и не я е поправило. Хеширането на паролите е заместител, отбелязан в изходния текст
като негоден за употреба.

Истинската проверка на използваемостта изисква независим потребител, и тя не е извършена.
Записва се като ограничение, а не като бъдеща работа, защото бъдещата работа звучи като
обещание, а това е липса.

\subsection{\bgen{Изводи}{Summary}}

Приложението показва, че трите архитектурни решения се понасят взаимно в код, който не е
измерителен стенд: handler-ите не знаят по кой протокол е дошла заявката, изгледите вземат
данни от същите маршрути като външните клиенти без излизане от процеса, а отложеното поле е
добавка от два реда към обикновена структура, само единият от които говори за отлагане.

Приложението намери и три неща, които не работят: маршрут, преименуван заради параметър;
удостоверяване, което не се вписа в единствения предвиден начин за композиция; канал за
известия, който се допитва вместо да разпраща. Нито едно от трите не се вижда от страната
на реализацията.

Това е по-слабо твърдение от „библиотеката е лесна за използване“ и е точно толкова,
колкото един автор може да провери сам за собствената си работа.
