*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz

# build/ is latexmk's working directory and is not committed. The finished PDF is: latexmkrc
# copies build/main.pdf to main.pdf after every successful build, and main.pdf is versioned
# so the document can be read without a TeX installation (asked for on 2026-09-02).
build/

# Written by tools/results2tex.py from the measurement output. Regenerable by
# definition, and committing it would let a stale number outlive the run it came from.
generated/
