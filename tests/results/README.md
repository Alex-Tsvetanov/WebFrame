# Integration test raw output

`http3_path_migration.log` is the transcript of a real run of
`tests/integration/verify_http3_migration.sh` (also registered as ctest
`http3_path_migration`). Numbers in that file are from the machine that produced
it; do not invent replacements.
